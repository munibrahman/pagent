import Dashboard from "../components/Dashboard";

export const metadata = {
  title: "PartsPanda — Live Dashboard",
  description: "Live call activity, inventory hits, and captured leads.",
};

export default function DashboardPage() {
  return <Dashboard />;
}
